# Overlay is click-through except during display-region adjust

The overlay sits on the game. Always-draggable subtitles steal clicks meant for in-game controls. Display-region adjust is armed per pair from settings; only that pair's display region then takes the mouse, and both rectangles of the pair are outlined. Otherwise the overlay — subtitles, hints, and the region-pair preview — is click-through.
